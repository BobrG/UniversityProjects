#include "Ecosys.h"

void func(double tmp) {
	tmp += 10;
}

int main() {
	//std::string str = "room";
	//Container* Room = new Container(str);
	//str = "bottle";
	//Container* Bottle = new Container(str);
	//Room->add_container(Bottle);
	Modifier<int> *tmp = new Modifier<int>;
	//Temperature->Init("temperature",func,"temperature");
	//Room->add_modifier(Temperature);

}